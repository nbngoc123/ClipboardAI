<UserControl x:Class="ClipboardAI.Views.Settings.GeneralSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600"
             Background="Transparent">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="24,20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="⚙️" FontSize="28" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock Text="General" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="App-wide preferences and startup behavior." Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="14" Margin="0,0,0,24"/>

            <TextBlock Text="STARTUP" Style="{StaticResource PtSectionTitle}"/>
            
            <Border Style="{StaticResource PtSectionCard}">
                <StackPanel>
                    <!-- Launch at startup -->
                    <Border BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="0,0,0,0.5" Padding="20,16">
                        <Grid>
                            <StackPanel VerticalAlignment="Center" Margin="0,0,80,0">
                                <TextBlock Text="Launch at startup" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                                <TextBlock Text="Start ClipboardAI when Windows boots" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                            </StackPanel>
                            <CheckBox Style="{StaticResource PtToggleSwitch}" IsChecked="{Binding LaunchAtStartup, Mode=TwoWay}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <!-- Start as administrator -->
                    <Border Padding="20,16">
                        <Grid>
                            <StackPanel VerticalAlignment="Center" Margin="0,0,80,0">
                                <TextBlock Text="Start as administrator" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                                <TextBlock Text="Required for some hotkey features to work globally" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                            </StackPanel>
                            <CheckBox Style="{StaticResource PtToggleSwitch}" IsChecked="{Binding StartAsAdmin, Mode=TwoWay}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <TextBlock Text="APPEARANCE" Style="{StaticResource PtSectionTitle}"/>
            <Border Style="{StaticResource PtSectionCard}">
                <StackPanel>
                    <Border Padding="20,16">
                        <Grid>
                            <StackPanel VerticalAlignment="Center" Margin="0,0,180,0">
                                <TextBlock Text="App theme" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                                <TextBlock Text="Override system theme" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                            </StackPanel>
                            <!-- Note: A proper two-way binding would use SelectedItem bound to AppTheme, 
                                 or a converter. For simplicity, we bind SelectedValue. -->
                            <ComboBox Width="160" Height="32" HorizontalAlignment="Right" VerticalAlignment="Center" Background="White" Foreground="Black" BorderBrush="{StaticResource PtBorderBrush}" SelectedValue="{Binding AppTheme, Mode=TwoWay}" SelectedValuePath="Content">
                                <ComboBoxItem Content="Light"/>
                                <ComboBoxItem Content="Dark"/>
                                <ComboBoxItem Content="Windows default"/>
                            </ComboBox>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <TextBlock Text="STORAGE &amp; LIMITS" Style="{StaticResource PtSectionTitle}"/>
            <Border Style="{StaticResource PtSectionCard}">
                <StackPanel>
                    <Border Padding="20,16">
                        <Grid>
                            <StackPanel VerticalAlignment="Center" Margin="0,0,160,0">
                                <TextBlock Text="Maximum history items" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                                <TextBlock Text="Oldest items will be deleted automatically" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBox Text="{Binding MaxHistoryItems, UpdateSourceTrigger=PropertyChanged}" Width="60" Height="30" FontSize="14" Padding="8,4" VerticalContentAlignment="Center" Background="#1F1F1F" Foreground="White" CaretBrush="White" BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="1"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
