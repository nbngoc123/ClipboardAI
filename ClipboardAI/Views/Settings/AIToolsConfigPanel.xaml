<UserControl x:Class="ClipboardAI.Views.Settings.AIToolsConfigPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:ClipboardAI.ViewModels.Settings"
             d:DataContext="{d:DesignInstance Type=viewmodels:AIToolsConfigViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600"
             Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="24,20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="⚙️" FontSize="28" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock Text="AI Tool Behaviors" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Configure languages and instructions for AI features." Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="14" Margin="0,0,0,24"/>

            <TextBlock Text="DATA EXTRACTION" Style="{StaticResource PtSectionTitle}"/>
            <Border Style="{StaticResource PtSectionCard}" Margin="0,0,0,24">
                <StackPanel>
                    <Grid Margin="20,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Target Language" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                            <TextBlock Text="The language to extract data in" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                        </StackPanel>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableLanguages}" SelectedItem="{Binding ExtractLanguage}" VerticalAlignment="Center"/>
                    </Grid>
                    
                    <Border BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="0,1,0,0"/>
                    <StackPanel Margin="20,16">
                        <TextBlock Text="Custom Prompt / Instructions" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                        <TextBlock Text="Add specific rules for the AI (e.g. 'Focus only on names and emails')" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,8"/>
                        <TextBox Text="{Binding ExtractCustomPrompt}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <TextBlock Text="TRANSLATE &amp; SUMMARIZE" Style="{StaticResource PtSectionTitle}"/>
            <Border Style="{StaticResource PtSectionCard}" Margin="0,0,0,24">
                <StackPanel>
                    <Grid Margin="20,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Summary Language" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                            <TextBlock Text="The language used for summarizing content" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                        </StackPanel>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableLanguages}" SelectedItem="{Binding SummaryLanguage}" VerticalAlignment="Center"/>
                    </Grid>
                    
                    <Border BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="0,1,0,0"/>
                    <Grid Margin="20,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Translation Language" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                            <TextBlock Text="The language to translate the content into" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                        </StackPanel>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableLanguages}" SelectedItem="{Binding TranslationLanguage}" VerticalAlignment="Center"/>
                    </Grid>

                    <Border BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="0,1,0,0"/>
                    <Grid Margin="20,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Writing Tone" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                            <TextBlock Text="The tone of the generated text" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                        </StackPanel>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableTones}" SelectedItem="{Binding AiTone}" VerticalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Save Button -->
            <Button Content="Save Config" Command="{Binding SaveCommand}" HorizontalAlignment="Right" Padding="24,8" Background="{StaticResource PtAccentBrush}" Foreground="White" BorderThickness="0" Cursor="Hand" Margin="0,0,0,24">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </ScrollViewer>
</UserControl>
