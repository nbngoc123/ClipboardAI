<UserControl x:Class="ClipboardAI.Views.Settings.HotkeySettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600"
             Background="Transparent">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="24,20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="⌨️" FontSize="28" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock Text="Keyboard Manager" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Remap individual keys and shortcut combinations." Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="14" Margin="0,0,0,24"/>

            <Border Style="{StaticResource PtSectionCard}" Margin="0,0,0,24">
                <Border Padding="20,16">
                    <Grid>
                        <StackPanel VerticalAlignment="Center" Margin="0,0,80,0">
                            <TextBlock Text="Enable Keyboard Manager" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                            <TextBlock Text="Requires running as administrator" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                        </StackPanel>
                        <CheckBox Style="{StaticResource PtToggleSwitch}" IsChecked="{Binding EnableKeyboardManager, Mode=TwoWay}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </Border>

            <Border Background="#102A45" BorderBrush="{StaticResource PtAccentBrush}" BorderThickness="1" CornerRadius="4" Padding="16" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🛡️" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Keyboard remapping requires PowerToys to run as administrator. Some keys may not be remappable in all applications." Foreground="{StaticResource PtTextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <TextBlock Text="REMAPPED KEYS" Style="{StaticResource PtSectionTitle}"/>
            <Border Style="{StaticResource PtSectionCard}">
                <StackPanel>
                    <Border BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="0,0,0,0.5" Padding="20,16">
                        <Grid>
                            <TextBlock Text="Open Clipboard Popup" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}" VerticalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Border Style="{StaticResource PtKeyboardBadge}">
                                    <TextBlock Text="{Binding OpenPopupHotkey}" Style="{StaticResource PtKeyboardBadgeText}"/>
                                </Border>
                                <Button Content="Edit" Command="{Binding EditOpenPopupHotkeyCommand}" Foreground="{StaticResource PtAccentBrush}" Background="Transparent" BorderThickness="0" Margin="16,0,0,0" Cursor="Hand"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <Border BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="0,0,0,0.5" Padding="20,16">
                        <Grid>
                            <TextBlock Text="Toggle Batch Mode" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}" VerticalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Border Style="{StaticResource PtKeyboardBadge}">
                                    <TextBlock Text="{Binding ToggleBatchCopyHotkey}" Style="{StaticResource PtKeyboardBadgeText}"/>
                                </Border>
                                <Button Content="Edit" Command="{Binding EditToggleBatchCopyHotkeyCommand}" Foreground="{StaticResource PtAccentBrush}" Background="Transparent" BorderThickness="0" Margin="16,0,0,0" Cursor="Hand"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <Border Padding="20,16">
                        <Button Content="Add remap" HorizontalAlignment="Left" Background="#2B2B2B" Foreground="White" BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="1" Padding="12,6"/>
                    </Border>
                </StackPanel>
            </Border>
            
        </StackPanel>
    </ScrollViewer>
</UserControl>
