<UserControl x:Class="ClipboardAI.Views.Settings.DashboardPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600"
             Background="Transparent">
    
    <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled" Padding="24,20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="🏠" FontSize="28" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock Text="Dashboard" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Overview of all utilities and their status." Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="14" Margin="0,0,0,24"/>

            <!-- 3 Stat Cards like PowerToys -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Background="{StaticResource PtCardBrush}" BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Enabled" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,0,0,8"/>
                        <TextBlock Text="3" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}" Margin="0,0,0,8"/>
                        <TextBlock Text="of 4 utilities" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Column="2" Background="{StaticResource PtCardBrush}" BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Version" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,0,0,8"/>
                        <TextBlock Text="2.0.0" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}" Margin="0,0,0,8"/>
                        <TextBlock Text="up to date" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Column="4" Background="{StaticResource PtCardBrush}" BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Hotkeys" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,0,0,8"/>
                        <TextBlock Text="3" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}" Margin="0,0,0,8"/>
                        <TextBlock Text="remapped" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Info Bar -->
            <Border Background="#102A45" BorderBrush="{StaticResource PtAccentBrush}" BorderThickness="1" CornerRadius="4" Padding="16" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="ℹ️" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Welcome to ClipboardAI! Use the settings below to customize your experience." Foreground="{StaticResource PtTextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <TextBlock Text="QUICK STATUS" Style="{StaticResource PtSectionTitle}"/>
            
            <Border Style="{StaticResource PtSectionCard}">
                <StackPanel>
                    <Border BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="0,0,0,0.5" Padding="20,16">
                        <Grid>
                            <StackPanel>
                                <TextBlock Text="Start with Windows" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                                <TextBlock Text="Automatically launch on boot" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                            </StackPanel>
                            <TextBlock Text="{Binding SettingsViewModel.GeneralSettings.LaunchAtStartup}" Foreground="{StaticResource PtStatusEnabledBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        </Grid>
                    </Border>
                    <Border BorderBrush="{StaticResource PtBorderBrush}" BorderThickness="0,0,0,0.5" Padding="20,16">
                        <Grid>
                            <StackPanel>
                                <TextBlock Text="Keyboard Manager" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                                <TextBlock Text="Custom hotkey remapping" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                            </StackPanel>
                            <TextBlock Text="{Binding SettingsViewModel.HotkeySettings.EnableKeyboardManager}" Foreground="{StaticResource PtStatusEnabledBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        </Grid>
                    </Border>
                    <Border Padding="20,16">
                        <Grid>
                            <StackPanel>
                                <TextBlock Text="Theme Override" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource PtTextPrimaryBrush}"/>
                                <TextBlock Text="Current application theme" Foreground="{StaticResource PtTextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
                            </StackPanel>
                            <TextBlock Text="{Binding SettingsViewModel.GeneralSettings.AppTheme}" Foreground="{StaticResource PtAccentBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
